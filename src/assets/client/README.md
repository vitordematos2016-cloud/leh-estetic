# src/assets/client

Logotipo e materiais de identidade visual enviados pela cliente (logo, ícones de marca). Depois de colocar o arquivo aqui, importe-o no componente correspondente e passe o caminho pelo `siteContent` (ex: `company.heroImage`).
