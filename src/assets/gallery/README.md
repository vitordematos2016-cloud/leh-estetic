# src/assets/gallery

Fotos do espaço, equipamentos, procedimentos, produtos, equipe e resultados autorizados. Importe cada arquivo em `src/data/siteContent.ts` e atribua ao campo `image` do item correspondente em `gallery`, depois ative `settings.showGallery`.
